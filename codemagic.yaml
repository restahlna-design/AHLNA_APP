workflows:
  ios-app-store-release:
    name: iOS App Store Connect Official Release
    max_build_duration: 120
    integrations:
      app_store_connect: Ahlna Key
    environment:
      flutter: stable
      xcode: latest
      groups:
        - supabase
        - app_store_credentials
      vars:
        FLUTTER_VERSION: "3.24.5"
        CM_NETWORK_TIMEOUT: 600
        APP_STORE_APPLE_ID: 6804920766
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: "main"
          include: true
          source: true
    scripts:
      - name: Pre-build setup
        script: |
          echo "== Pre-build network optimization =="
          export CURL_TIMEOUT=600
          export CURL_CONNECT_TIMEOUT=30
          export FLUTTER_STORAGE_BASE_URL=https://storage.googleapis.com

      - name: Clean previous artifacts and dependencies
        script: |
          echo "== Clean previous build artifacts =="
          flutter clean
          flutter pub get
          cd ios
          pod deintegrate || true
          pod install --repo-update || pod install
          cd ..

      - name: Create or Fetch Official Apple Distribution Certificate via Ahlna Key
        script: |
          set -e
          echo "== Initializing clean keychain =="
          keychain initialize

          echo "== Existing Apple Certificates on Developer Account =="
          app-store-connect certificates list || true

          echo "== Generating 2048-bit RSA Private Key =="
          CERT_KEY="/Users/builder/cert_key.pem"
          openssl genrsa -out "$CERT_KEY" 2048

          echo "== Creating Fresh Official Apple Distribution Certificate =="
          app-store-connect certificates create \
            --type IOS_DISTRIBUTION \
            --certificate-key "$CERT_KEY" \
            --p12-password "codemagic" \
            --p12-path "/Users/builder/dist_cert.p12" \
            --save || echo "Create certificate returned with message"

          echo "== Fetching or Generating App Store Provisioning Profile =="
          app-store-connect fetch-signing-files "com.example.ahlnaDaquq" \
            --type IOS_APP_STORE \
            --create \
            --certificate-key "$CERT_KEY" || true

          echo "== Adding Valid Certificates to Keychain =="
          if [ -f "/Users/builder/dist_cert.p12" ]; then
            keychain add-certificates --certificate "/Users/builder/dist_cert.p12" --certificate-password "codemagic"
          fi
          keychain add-certificates || true

          echo "== Verifying Installed Identities in Keychain =="
          security find-identity -v -p codesigning || true

          echo "== Configuring Xcode Project Profiles =="
          xcode-project use-profiles

      - name: Build Official Signed Release IPA (Customer App)
        script: |
          set -eo pipefail
          export SUPABASE_URL="${SUPABASE_URL:-https://boylzidmvvldouxtrpiv.supabase.co}"
          export SUPABASE_ANON_KEY="${SUPABASE_ANON_KEY:-}"

          echo "== Git Build Information =="
          echo "Commit SHA: $(git rev-parse HEAD)"
          echo "Flutter Version: $(flutter --version | head -n 1)"
          echo "Xcode Version: $(xcodebuild -version | head -n 1)"
          echo "Build Time: $(date)"

          EXPORT_PLIST="${HOME}/export_options.plist"
          if [ ! -f "$EXPORT_PLIST" ]; then
            EXPORT_PLIST="/Users/builder/export_options.plist"
          fi
          
          echo "== Export Options Plist Content =="
          cat "$EXPORT_PLIST" || true

          flutter build ipa --release \
            --target lib/main.dart \
            --export-options-plist "$EXPORT_PLIST" \
            --dart-define=SUPABASE_URL="${SUPABASE_URL}" \
            --dart-define=SUPABASE_ANON_KEY="${SUPABASE_ANON_KEY}"

          echo "== Final Artifact Verification =="
          ls -lah build/ios/ipa/*.ipa || true
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false

  ios_app_store_release:
    name: iOS App Store Connect Official Release (Legacy Alias)
    max_build_duration: 120
    integrations:
      app_store_connect: Ahlna Key
    environment:
      flutter: stable
      xcode: latest
      groups:
        - supabase
        - app_store_credentials
      vars:
        FLUTTER_VERSION: "3.24.5"
        CM_NETWORK_TIMEOUT: 600
        APP_STORE_APPLE_ID: 6804920766
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: "main"
          include: true
          source: true
    scripts:
      - name: Pre-build setup
        script: |
          echo "== Pre-build network optimization =="
          export CURL_TIMEOUT=600
          export CURL_CONNECT_TIMEOUT=30
          export FLUTTER_STORAGE_BASE_URL=https://storage.googleapis.com

      - name: Clean previous artifacts and dependencies
        script: |
          echo "== Clean previous build artifacts =="
          flutter clean
          flutter pub get
          cd ios
          pod deintegrate || true
          pod install --repo-update || pod install
          cd ..

      - name: Create or Fetch Official Apple Distribution Certificate via Ahlna Key
        script: |
          set -e
          echo "== Initializing clean keychain =="
          keychain initialize

          echo "== Existing Apple Certificates on Developer Account =="
          app-store-connect certificates list || true

          echo "== Generating 2048-bit RSA Private Key =="
          CERT_KEY="/Users/builder/cert_key.pem"
          openssl genrsa -out "$CERT_KEY" 2048

          echo "== Creating Fresh Official Apple Distribution Certificate =="
          app-store-connect certificates create \
            --type IOS_DISTRIBUTION \
            --certificate-key "$CERT_KEY" \
            --p12-password "codemagic" \
            --p12-path "/Users/builder/dist_cert.p12" \
            --save || echo "Create certificate returned with message"

          echo "== Fetching or Generating App Store Provisioning Profile =="
          app-store-connect fetch-signing-files "com.example.ahlnaDaquq" \
            --type IOS_APP_STORE \
            --create \
            --certificate-key "$CERT_KEY" || true

          echo "== Adding Valid Certificates to Keychain =="
          if [ -f "/Users/builder/dist_cert.p12" ]; then
            keychain add-certificates --certificate "/Users/builder/dist_cert.p12" --certificate-password "codemagic"
          fi
          keychain add-certificates || true

          echo "== Verifying Installed Identities in Keychain =="
          security find-identity -v -p codesigning || true

          echo "== Configuring Xcode Project Profiles =="
          xcode-project use-profiles

      - name: Build Official Signed Release IPA (Customer App)
        script: |
          set -eo pipefail
          export SUPABASE_URL="${SUPABASE_URL:-https://boylzidmvvldouxtrpiv.supabase.co}"
          export SUPABASE_ANON_KEY="${SUPABASE_ANON_KEY:-}"

          echo "== Git Build Information =="
          echo "Commit SHA: $(git rev-parse HEAD)"
          echo "Flutter Version: $(flutter --version | head -n 1)"
          echo "Xcode Version: $(xcodebuild -version | head -n 1)"
          echo "Build Time: $(date)"

          EXPORT_PLIST="${HOME}/export_options.plist"
          if [ ! -f "$EXPORT_PLIST" ]; then
            EXPORT_PLIST="/Users/builder/export_options.plist"
          fi
          
          echo "== Export Options Plist Content =="
          cat "$EXPORT_PLIST" || true

          flutter build ipa --release \
            --target lib/main.dart \
            --export-options-plist "$EXPORT_PLIST" \
            --dart-define=SUPABASE_URL="${SUPABASE_URL}" \
            --dart-define=SUPABASE_ANON_KEY="${SUPABASE_ANON_KEY}"

          echo "== Final Artifact Verification =="
          ls -lah build/ios/ipa/*.ipa || true
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false

  android_release:
    name: Android APK (Release)
    max_build_duration: 60
    environment:
      flutter: stable
      vars:
        ANDROID_HOME: /opt/android-sdk
      groups:
        - supabase
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: ".*"
          include: true
          source: true
    scripts:
      - name: Flutter clean & pub get
        script: |
          flutter clean
          flutter pub get
      - name: Build Android Release APK (env vars)
        script: |
          set -e
          flutter build apk --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
      - name: List APK artifacts
        script: |
          ls -lah build/app/outputs/flutter-apk/ || true
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      - build/**/**/app-release.apk
